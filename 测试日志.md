# 测试日志

> 用于记录 *Unicorn CAS* 高压斩波运放每日的代办与结果；

## 20260819

**测试内容：**

- 关于 `DUT` 低压（±3V）噪声测试的初步尝试；
- 解决输入偏置电流bug；
- 尝试针对示波器验证底噪；

**测试结论：**

- 示波器 PXI-5922 不能AC耦合，AC耦合会在低频出（~1Hz）处出现衰减；
- 示波器 PXI-5922 验证底噪不能 floating输入信号，这样测得的噪声要大很多，需要**短路帽**；
- DC 耦合测试结果在 1Hz处出现类似 flicking noise 的“翘起”（~10nV/rtHz），==暂时无法确定问题；==
- DC 耦合测试结果有较大的 DC offset，大概率是因为 `Vtest_noise` SMA存在双驱动问题（一边是 `DUT` 输出驱动，一边是辅助运放输出驱动）；

**下一步事项：**

1. 拆掉噪声辅助运放输出的那个50ohm电阻，将噪声辅助运放断路，以此验证 DC 耦合的测试结果中offset是否存在；
2. 完成不同片子、不同增益下的完整噪声频谱；

|测试条件|具体细则| N | 
|---|---|---|
|`DUT`|5块 COB| x5 |
|测试频段|1Hz -> 1kHz,1KHz->10kHz,10kHz -> 10MHz| x3|
|输入偏置电流|1uA, 2uA, 4uA| x3|
|**合计**| **average x10**|**450** | 

注意事项： 
   - 给 COB 编号；
   - 数据保存编号 or 命名；
   - 1Hz -> 1kHz 用 Gain = 1001放大，示波器 PXI-5922；
   - 1kHz -> 10kHz 用 Gain = 101放大，示波器 PXI-5922；
   - 10kHz -> 10MHz 用Gain= 1放大，示波器 泰克；
   - 手动记录一下每个片子给不同偏置电流后，电流源的表显电压；